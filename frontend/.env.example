# API backend (clips / live / feedback)
VITE_API_URL=http://127.0.0.1:8000

# Canonical public origin for SEO (no trailing slash). Used for canonical/OG URLs
# and to write absolute Sitemap entries on `vite build`.
VITE_SITE_URL=https://www.isocourt.fit
