# Local API — inference category comes from deploy/docker-inference.env (CLI: inference_model_cli set)
services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "7860:7860"
    env_file:
      - deploy/docker-inference.env
