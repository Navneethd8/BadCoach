# VLM fine-tuning (Qwen3-VL + Unsloth). Use a GPU env (conda isocourt on cluster).
#
# If bootstrap_cluster already installed torch 2.5.x, install Unsloth FIRST, then
# realign the torch triplet (avoids torchaudio 2.5 vs torch 2.10 warnings):
#
#   pip install -r backend/pipelines/vlm/common/requirements-unsloth-vlm.txt
#   pip install torch==2.10.0 torchvision==0.25.0 torchaudio==2.10.0 \
#     --index-url https://download.pytorch.org/whl/cu124
#
# Pick cu124 (driver >=550) or cu121 (driver >=525). Verify:
#   python -c "import torch; print(torch.__version__, torch.cuda.is_available())"

unsloth
transformers>=4.57.0
trl>=0.22.0
datasets>=2.16.0
accelerate>=0.34.0
bitsandbytes>=0.43.0
peft>=0.12.0
sentencepiece
protobuf
pillow
mediapipe
opencv-python
