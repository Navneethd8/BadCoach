# Cluster nodes have no libGL — use headless only (never opencv-python).
# After any pip install in isocourt-mmpose, run: ./scripts/cluster/ensure_opencv_headless.sh

setuptools>=68
wheel
numpy==1.26.4
opencv-python-headless==4.9.0.80
tqdm
xtcocotools>=1.14
openmim
mmengine>=0.10.0
mmdet>=3.2.0,<3.4.0
mmpose>=1.3.0,<1.4.0
